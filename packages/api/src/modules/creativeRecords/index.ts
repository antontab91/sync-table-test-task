import * as actions from './actions';
import * as queries from './queries';

const creativeRecords = {
    actions,
    queries,
};

export default creativeRecords;

export * from './types';
export { actions, queries };
